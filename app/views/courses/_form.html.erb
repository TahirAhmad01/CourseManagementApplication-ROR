<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :course_name, style: "display: block" %>
    <%= form.text_field :course_name %>
  </div>

<!--  <div>-->
    <%#= form.label :semester_id, style: "display: block" %>
    <%#= form.text_field :semester_id %>
<!--  </div>-->

  <div class="field col-6 mb-3" >
    <%= form.label :semester_id %><br/>
    <%= form.collection_select :semester_id, Semester.all, :id, :semester_name, { prompt: "Select your semester" }, { class: "form-select form-select-md text-capitalize" } %>
  </div>

  <div class="mb-3">
<!--    <label for="formFile" class="form-label">Default file input example</label>-->
    <%= form.label :course_image , class: "form-label" %>
    <%= form.file_field :course_image, class: "form-control"%>
<!--    <input class="form-control" type="file" id="formFile">-->
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
