.d-flex.gap-4
.card.mb-3.w-100[style="max-height: 170px"]
  .row.g-0
    .col-md-2
      - if course.course_image.attached?
        = image_tag course.course_image, class: "img-fluid rounded-start w-100 h-100 object-fit-cover", style: "height: 100%"
      - else
        = image_tag "no-image.png", class: "img-fluid rounded-start w-100 h-100 object-fit-cover"
    .col-md-10
      .card-body
        h5.card-title
          = course.course_name
        small
          | Credit: 
          = course.credit
          |  - 
          = course.semester.semester_name
        p.card-text.text-secondary
          - if course.description?
            = course.description
          - else
            |  This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer. 
        .d-flex
          .px-1
            = link_to "Edit", edit_course_path(course.id), class: "btn btn-primary w-100 btn-sm px-4"
          .px-1
            = button_to "Destroy", course_path(course), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-danger w-100 px-3"
